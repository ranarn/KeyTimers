# Normalize all text files to LF in the repo and on checkout
* text=auto eol=lf

# Explicitly mark binary files
*.exe binary
*.zip binary
*.dll binary
*.pdb binary
*.png binary
*.ico binary
*.wav binary
*.mp3 binary
